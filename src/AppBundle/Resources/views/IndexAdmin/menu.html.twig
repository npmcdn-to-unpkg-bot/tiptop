<ul class="nav nav-pills">
    {% for item in items %}
        <li role="presentation" class="{% if routeName == item.path %}active{% endif %}">
            <a href="{{ path(item.path) }}">{{ item.name }}</a>
        </li>
    {% endfor %}
</ul>